[phases.setup]
nixPkgs = ["python311", "gcc", "glib", "nss", "atk", "gtk3", "libx11", "libxcomposite", "libxdamage", "libxrandr", "libgbm", "libasound"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -m playwright install chromium"
]

[phases.start]
cmd = "python main.py"
