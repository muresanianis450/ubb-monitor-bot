[phases.setup]
nixPkgs = ["python313", "gcc", "glib", "nss", "atk", "gtk3", "libx11", "libxcomposite", "libxdamage", "libxrandr", "libgbm", "libasound"]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[phases.start]
cmd = "playwright install chromium && python main.py"